import {Configs} from './default/_config';
import Styles from './default/_index';

const Themes = {
  Styles,
  Color: Configs.Color,
  FontSize: Configs.FontSize,
  FontWeight: Configs.FontWeight,
};

export default Themes;
